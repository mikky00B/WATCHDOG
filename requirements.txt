# ─────────────────────────────────────────────────────────
# HOW TO INSTALL (choose one):
#
#   OPTION 1 — recommended (installs package in editable mode):
#     pip install -r requirements.txt && pip install -e .
#
#   OPTION 2 — set PYTHONPATH instead:
#     pip install -r requirements.txt
#     PYTHONPATH=src uvicorn monitoring.main:app --reload
#
#   OPTION 3 — Poetry (handles everything automatically):
#     poetry install && poetry run uvicorn monitoring.main:app --reload
# ─────────────────────────────────────────────────────────

# Core dependencies
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
pydantic-settings==2.1.0
sqlalchemy==2.0.25
asyncpg==0.29.0
alembic==1.13.1
httpx==0.26.0
structlog==24.1.0
python-multipart==0.0.6
setuptools>=68.0.0
aiosqlite==0.19.0

# Development dependencies
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0
mypy==1.8.0
ruff==0.1.14
autoflake==2.2.1
radon==6.0.1
pre-commit==3.6.0
